# Application Configuration
# Format: [app_name]
# category=category_key
# display_name=Human Readable Name
# description=Short description
# requires_domain=yes|no
# requires_ssl=yes|no
# dependencies=dep1,dep2
# default_port=port_number
# secrets=SECRET1,SECRET2

[postgres]
category=databases
display_name=PostgreSQL
description=Powerful relational database with ACID compliance
requires_domain=no
requires_ssl=no
dependencies=docker-engine
default_port=5432
secrets=DB_PASSWORD,POSTGRES_USER

[mariadb]
category=databases
display_name=MariaDB
description=MySQL-compatible relational database
requires_domain=no
requires_ssl=no
dependencies=docker-engine
default_port=3306
secrets=MYSQL_ROOT_PASSWORD,MYSQL_PASSWORD

[mongodb]
category=databases
display_name=MongoDB
description=NoSQL document-oriented database
requires_domain=no
requires_ssl=no
dependencies=docker-engine
default_port=27017
secrets=MONGO_INITDB_ROOT_PASSWORD

[redis]
category=databases
display_name=Redis
description=In-memory data structure store and cache (native)
requires_domain=no
requires_ssl=no
dependencies=
default_port=6379
secrets=REDIS_PASSWORD

[n8n]
category=automation
display_name=n8n
description=Workflow automation platform with visual editor
requires_domain=yes
requires_ssl=yes
dependencies=docker-engine,postgres,redis,nginx
default_port=5678
secrets=N8N_DB_PASSWORD,N8N_ENCRYPTION_KEY

[arcane]
category=infrastructure
display_name=Arcane
description=Modern Docker management with real-time monitoring
requires_domain=yes
requires_ssl=yes
dependencies=docker-engine,nginx,redis
default_port=3000
secrets=ADMIN_PASSWORD,API_TOKEN

[grafana]
category=monitoring
display_name=Grafana
description=Analytics and interactive visualization platform
requires_domain=yes
requires_ssl=yes
dependencies=docker-engine
default_port=3000
secrets=GF_SECURITY_ADMIN_PASSWORD

[prometheus]
category=monitoring
display_name=Prometheus
description=Monitoring system and time series database
requires_domain=no
requires_ssl=no
dependencies=docker-engine
default_port=9090
secrets=

[netdata]
category=monitoring
display_name=Netdata
description=Real-time performance and health monitoring
requires_domain=yes
requires_ssl=yes
dependencies=docker-engine
default_port=19999
secrets=

[uptime-kuma]
category=monitoring
display_name=Uptime Kuma
description=Self-hosted monitoring tool
requires_domain=yes
requires_ssl=yes
dependencies=docker-engine
default_port=3001
secrets=

[docker-engine]
category=infrastructure
display_name=Docker Engine
description=Container runtime platform
requires_domain=no
requires_ssl=no
dependencies=
default_port=
secrets=

[portainer]
category=infrastructure
display_name=Portainer
description=Docker management web interface
requires_domain=yes
requires_ssl=yes
dependencies=docker-engine
default_port=9000
secrets=PORTAINER_ADMIN_PASSWORD

[nginx]
category=infrastructure
display_name=Nginx
description=High-performance web server and reverse proxy (native)
requires_domain=no
requires_ssl=no
dependencies=
default_port=80,443
secrets=

[certbot]
category=infrastructure
display_name=Certbot
description=Automatic SSL certificate management (native)
requires_domain=yes
requires_ssl=no
dependencies=nginx
default_port=
secrets=

[wireguard]
category=security
display_name=WireGuard VPN
description=Modern and secure VPN solution (native)
requires_domain=no
requires_ssl=no
dependencies=
default_port=51820
secrets=WG_PASSWORD

[security-audit]
category=security
display_name=Security Audit
description=Automated security scanning and hardening
requires_domain=no
requires_ssl=no
dependencies=
default_port=
secrets=

[fail2ban]
category=security
display_name=Fail2ban
description=Intrusion prevention system
requires_domain=no
requires_ssl=no
dependencies=
default_port=
secrets=

[setup-vps]
category=system
display_name=VPS Initial Setup
description=Initial VPS configuration and hardening
requires_domain=no
requires_ssl=no
dependencies=
default_port=
secrets=

[nodejs]
category=system
display_name=NodeJS Runtime
description=JavaScript runtime environment
requires_domain=no
requires_ssl=no
dependencies=
default_port=
secrets=

[log-maintenance]
category=system
display_name=Log Maintenance
description=Automated log rotation and cleanup
requires_domain=no
requires_ssl=no
dependencies=
default_port=
secrets=

[ollama]
category=ai
display_name=Ollama
description=Self-hosted LLM runtime (Llama, Mistral, CodeLlama)
requires_domain=no
requires_ssl=no
dependencies=docker-engine
default_port=11434
secrets=DEFAULT_MODEL

[llama-cpp]
category=ai
display_name=llama.cpp
description=Flexible LLM runtime with HuggingFace auto-download (any GGUF model)
requires_domain=no
requires_ssl=no
dependencies=docker-engine
default_port=8080
secrets=
